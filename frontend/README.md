# Metadata-Curator Frontend

The frontend is written with vue.js. More information is available in the main README for this repo.

To install dependencies `npm install`
To run this run `npm run serve`